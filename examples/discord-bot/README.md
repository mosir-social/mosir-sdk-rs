# discord-bot example (placeholder)

This example will demonstrate a long-running public SSE subscription using `mosir-sdk-rs`.

Planned notes:

- reconnect logic should live in the app layer, not in the SDK
- document the 1-hour connection limit and restart behavior
- show graceful shutdown + resume strategy
